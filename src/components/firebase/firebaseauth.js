import React, { useState } from 'react';
import firebase from './firebase'; // Relative import from the same directory

const AuthExample = () => {
  // ... rest of your component code
};

export default AuthExample;
